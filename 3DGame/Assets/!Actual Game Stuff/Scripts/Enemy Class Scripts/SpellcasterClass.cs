using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpellcasterClass : BaseEnemy {

    // Level = (scales with area in which the enemy is found)

    // Health = 5/10 + (1 * level)
    // Physical Damage = 1/10 + (1 * level)
    // Physical Resist = 1/10 + (1 * level)
    // Tech Damage = 10/10 + (3 * level)
    // Tech Resist = 8/10 + (2 * level)

}
