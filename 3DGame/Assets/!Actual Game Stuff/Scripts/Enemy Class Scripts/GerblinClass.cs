using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GerblinClass : BaseEnemy {

    // Level = (scales with area in which the enemy is found)

    // Health = 5/10 + (2 * level)
    // Physical Damage = 8/10 + (3 * level)
    // Physical Resist = 5/10 + (2 * level)
    // Tech Damage = 5/10 + (1 * level)
    // Tech Resist = 3/10 + (1 * level)

}
